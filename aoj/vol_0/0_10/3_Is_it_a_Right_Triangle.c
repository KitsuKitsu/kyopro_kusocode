#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    for(int i=0;i<n;i++){
        int num[2];
        scanf("%d %d %d", &num[0], &num[1], &num[2]);
        int syahen;
        for (int j = 0; j<3; j++){
            if(num[i] > num[j]){
                
            }
        }
    }
    return 0;
}
